{
  "name": "open-pinescript",
  "version": "0.0.1",
  "scripts": {
    "generate:parser": "npm run generate:parser:v1 && npm run generate:parser:v2 && npm run generate:parser:v3 && npm run generate:parser:v4 && node scripts/patch-visitor.js",
    "generate:parser:v1": "antlr-ng -Dlanguage=TypeScript -o parser/v1/generated -l false -v true --lib grammar grammar/PineV1Lexer.g4 grammar/PineV1Parser.g4",
    "generate:parser:v2": "antlr-ng -Dlanguage=TypeScript -o parser/v2/generated -l false -v true --lib grammar grammar/PineV2Lexer.g4 grammar/PineV2Parser.g4",
    "generate:parser:v3": "antlr-ng -Dlanguage=TypeScript -o parser/v3/generated -l false -v true --lib grammar grammar/PineV3Lexer.g4 grammar/PineV3Parser.g4",
    "generate:parser:v4": "antlr-ng -Dlanguage=TypeScript -o parser/v4/generated -l false -v true --lib grammar grammar/PineV4Lexer.g4 grammar/PineV4Parser.g4",
    "postinstall": "npm run generate:parser",
    "prebuild": "ts-node scripts/generate-metadata.ts",
    "build": "npm run generate:parser",
    "parse": "tsx parser/v2/run_antlr.ts",
    "replv2": "tsx repl/v2/index.ts",
    "opsv2": "tsx mock_run/v2/run.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage"
  },
  "dependencies": {
    "antlr4ng": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "antlr-ng": "^1.0.0",
    "ts-morph": "^27.0.2",
    "ts-node": "^10.9.2",
    "tsx": "^4.21.0",
    "vitest": "^4.0.18"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
